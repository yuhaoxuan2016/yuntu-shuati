# 更新日志（刷题宝 PWA 网页版）

> 部署地址：https://YOUR_ENV_ID-YOUR_APPID.tcloudbaseapp.com
> 部署方式：`node scripts/deploy-hosting.mjs`（COS 直传，保留 1 个旧版本防 404）
> 部署前置条件：先 `npm run build` 产出 `dist/`，并确保 `scripts/.creds.env` 已配好腾讯云密钥
> （`TCB_SECRET_ID` / `TCB_SECRET_KEY`，缺失时脚本报「缺少腾讯云密钥」而不是静默失败）。
> 注意 `scripts/` 整目录被 gitignore（含本机凭据），仓库里看不到这些脚本，需本机自备。
> 版本号与 App「设置 → 更新 → 当前版本」「更新日志」模块保持一致

---

## [1.2.47] - 2026-08-23

### 🛡️ 关闭自由建公共题库 + 管理员审核
- **现状**：此前任何用户新建题库都可选「公共题库」，直接 `visibility:'public'` 上首页。
- **前端**：建库弹窗去掉「公共题库」选项（默认私人）；题库卡片菜单新增「🌍 提交到公共题库（待审核）」→ 状态置 `pending`。
- **云端 ACL**（quiz_banks/questions）：写规则收紧为「本人 且 `visibility != "public"`」——普通用户只能写 private/pending，公共库仅管理员（服务端脚本）可置 public。已备份原规则 `scripts/acl-backup-20260823.md`。
- **管理员审核脚本**：`scripts/review-banks.cjs`（列出 `pending` → `approve` 通过 → `reject` 拒绝）。
- **首页**：仍只展示 `visibility:'public'`（审核通过的）；`pending` 暂不公开，显示「⏳ 已提交待审核」。
- ⚠️ 注：云端 ACL 生效需安全规则缓存刷新（几分钟）；因当日云端读配额超限，生效验证待配额恢复后补充。前端流程不受影响。

---

## [1.2.46] - 2026-08-23

### 🧹 清理「我的题库」里的空公共题库壳
- **现状**：早期云同步把公共题库（合集/初级/中级/高级/技师）空壳拉进了本地「我的题库」（公共题目不进本地缓存，所以显示 0 题），占位但无内容。
- **修复**：
  - `pullCollection(quiz_banks)` 明确跳过公共题库（`visibility==='public'`），从源头杜绝空壳再同步进本地。
  - 首页加载后自动清理已存在的空壳（`cloud_shared===true` 且 0 题的题库），仅删本地、不影响云端公共数据和公共题库展示区。
- **效果**：「我的题库」区不再出现 0 题的空公共库卡；公共题库仍通过「🌍 公共题库」区云端直读，刷题不受影响。

---

## [1.2.45] - 2026-08-23

### ⚡ 云同步提速 + 拉取计数修复
- **计数修复**：`syncFromCloud` 的 `pulled` 改为只统计真正**新增**的题目（`writeLocal` 返回 `added/updated/skipped`），不再把"遍历的云端文档数"当拉取量。此前导入干净备份后显示「拉取 15800」，实际云端仅 14088，属统计误导。
- **性能优化**：`writeLocal` 的 `questions` 分支引入索引缓存（`cloudMap`：cloud_id→题目；`stemMap`：stem→题目），把"每道题都全量扫本地题库（O(n²)）"改为"O(1) Map 查询"。`mapCloudBankToLocal` 一次同步内复用题库列表，不再反复 `listBanks()`。同步显著提速。

---

## [1.2.44] - 2026-08-23

### 🐛 云同步推送侧 + 恢复导入防重复（闭环）
- **推送侧去重**：`pushToCloud` 的 `questions` 分支，对"无 synced_at 待推送"的题目按 `bank_ref + stem` 内容去重——本地若因历史叠加产生同题多份副本，只推内容唯一的一条，其余视为同源跳过，避免推送侧再次放大云端重复。
- **恢复导入补同步标记**：`restoreBackup` 导入备份时，给 `quiz_banks`/`questions` 自动补 `synced_at`（视为已同步），避免导入干净备份后立即点同步，把整库题目当"待推送"重复上传云端。
- **效果**：本地清理（导入去重备份）→ 推送不重复 → 拉取不叠加，云同步形成闭环。此前 v1.2.43 只修了拉取侧（writeLocal）去重，本版补齐推送侧 + 导入侧。

---

## [1.2.43] - 2026-08-23

### 🐛 云同步题目叠加根治（严重）
- **根因**：云端题库题目 `cloud_id` 全为空，本地「添加到我的题库」导入的题目也不带 `cloud_id`；`writeLocal` 拉取时用 `qCloudIdx.map.get(cloudId)` 匹配，key 是本地 `cloud_id`（空）→ 永远匹配不上 → 每次同步都把云端题当新题 `add` 进去 → **题目反复叠加、越点越多**（实测云端 private 题堆积 13945 条，各题库恰好翻倍）。
- **修复**：`writeLocal` 的 `questions` 分支升级为「bank_ref + stem」内容级幂等去重——无论 cloud_id 有无、是否匹配上，只要本地题库里已存在同样题干（stem）的题，就只更新（保留本地 id）而非新增。重复叠加从源头被阻断。
- **深挖背景**：此前 v1.2.40 加的 stem 去重只防「新增」，没清理云端已堆积的存量重复，且本地无 cloud_id 时仍可能漏判。本次同步清理云端存量（备份见 `backups/backup-2026-08-23T09-56-00-200Z`）。

---

## [1.2.42] - 2026-08-23

### 🐛 修复（全量代码审查发现）
- **deleteBank 级联删除遗漏**：删除题库时未清理 `review_records`（复习记录），导致删题库后复习记录残留。现补全 `review_records` 的按 `bank_id` 匹配删除。
- **judgeAnswerBool 空答案默认 'true'**：无法识别的判断词（空答案/乱码）不再默认返回 `'true'` 误判为正确，改为空串。
- **ExamTakeView reviewWrongs 类型声明**：补全 `myRaw`/`correctRaw` 字段，与 `getWrongQuestions` 返回值一致。
- **StatsView 错题饼图暗色主题适配**：中心圆和文字改用 CSS 变量（`--color-card`/`--color-text`），暗色主题下不再硬编码白色背景。

---

## [1.2.41] - 2026-08-23

### 🔧 全面代码审查
- **核心库 bug 扫描**：审查 `spaced-repetition.ts`、`db.ts`、`cloud.ts`、`exam.ts`、`ai.ts`、`parser.ts`、`api.ts` 共 7 个核心文件，覆盖逻辑错误、边界处理、类型一致性、竞态条件。
- **58 项单元测试全通过**：覆盖 SM-2 计算、自动评估（含边界 0ms/NaN/undefined）、记忆强度、标签映射、配额联动、距考天数、逾期判断、哈希去重、每日任务等全部核心函数。
- **移动端适配**：全部 15 个视图页面均有 `@media (max-width: 768px)` 断点；本次补全了首页记忆复习入口卡片和学习计划卡片的手机端布局（纵向堆叠、全宽健康条、隐藏箭头）。

### 📝 扫描结论
- 核心算法逻辑正确，边界处理完善，无严重 bug。
- 仍有 2 个中等问题待后续处理：`deleteBank` 级联删除未包含 `review_records`（删题库后复习记录残留）；`judgeAnswerBool` 空答案默认返回 `'true'`（可优化为空串）。
- `parser.ts` 的 `parseChineseNum` 对 '二十' 等复合数有轻微精度问题，但实际题库题号不会用到，影响可忽略。

---

## [1.2.40] - 2026-08-23

### 🐛 修复
- **云同步题目叠加**：修复 `writeLocal('questions', ...)` 在 `cloudId` 为空时，`qCloudIdx.map.get(null)` 只能匹配一个题目，其余全部被当作"新题"重复 add → 每次同步题目翻倍的问题。兜底方案：cloudId 为空时，按 `stem + bank_id` 内容去重；匹配成功后回写 `cloud_id` 到本地（下次同步可直接用 cloud_id 匹配，跳过 stem 比对）。

### 📱 移动端适配
- **MemoryReviewView 手机端完善**：scope-bar（题库选择器）改为可换行小屏适配；exam-banner/start-btn/exit-btn 全宽；due-head 纵向堆叠；memory-quality-bar 换行；overview/dist/due-item 间距优化。

---

## [1.2.39] - 2026-08-23

### 🔧 性能优化
- **calculateDailyTask O(N²) → O(N)**：将 `reviewRecords.find(r => r.question_id === question.id)` 嵌套搜索优化为 `recordMap.get(question.id)` Map 查表；同时 `plan.bankIds.includes()` 优化为 `bankIdSet.has()`。4000 题计算从 ~300ms 降到 **3ms**。

### 🐛 优化
- **记忆统计内容去重**：新增 `deduplicateByStem`——同 stem（题目内容）的题只保留记忆强度最高的那份记录，避免重复题（如低级/高级题库同题）导致健康度和强度分布虚高。概览区改为显示「去重后知识点数」。
- 新增 `simpleHash`（FNV-1a 变体）用于 stem 去重。

---

## [1.2.38] - 2026-08-23

### 🐛 修复（严重）
- **修复 `last_review` 存错为 `nextReview` 的 bug**：此前 `PracticeView.vue` 和 `MemoryReviewView.vue` 两处更新复习记录时，误将 `last_review` 设为 `result.nextReview`（下次应复习时间）而非 `now`（本次实际复习时间），导致：
  - `isStaleReview`（逾期重学判断）失效——刚复习完的题也会被误判为"逾期"
  - SM-2 间隔计算从错误的时间基准出发——复习后间隔被重复累加
- 现 `last_review` 正确记录本次复习时间，`next_review` 记录下次应复习时间，两者分离，SM-2 算法行为恢复正常。

---

## [1.2.37] - 2026-08-23

### ✨ 新功能
- **备考驱动动态复习节奏**：在学习计划里设置考试日期后，记忆复习的每日配额会按「距考试天数」动态调整：
  - 距考 > 30 天 → 8%（常规）
  - 距考 8–30 天 → 12%（加量复习）
  - 距考 3–8 天 → 18%（冲刺加量）
  - 距考 ≤ 3 天 → 25%（冲刺极限）
- 未设考试日期时保持默认常规节奏。
- 记忆复习页和首页显示「距最近考试 N 天」及对应复习阶段提示。

### 🔧 说明
- 取所有学习计划中**最早到期**的考试日期作为「距考天数」基准。
- 配额同样受「题库规模联动 + 保底25 + 上限200」约束，临近考试只会加量、不会失控堆积。

---

## [1.2.36] - 2026-08-23

### ✨ 新功能
- **记忆复习按题库独立**：记忆复习页顶部新增「全部 / 各题库」范围选择器，可单独复习某个题库，也可切回全部。

### 🔧 配额联动（解决大库 50 题太少的问题）
- 新增 `calculateBankReviewCap`——每个题库按题目规模联动每日配额：**配额 = clamp(题库题数 × 8%, 保底 25, 上限 200)**。
  - 4000 题大库 → 每天 200 题（原来全局才 50，提升 4 倍）
  - 1000 题 → 80 题；100 题 → 25 题保底
- 复习页概览区按当前选中范围显示建议题数；单库角标显示该库到期数。

---

## [1.2.35] - 2026-08-23

### ✨ 新功能
- **记忆复习防堆积机制**：新增每日复习配额（`calculateDailyReviewCap`）——联动学习计划（取各计划 `dailyGoal` 最大值），且至少有 50 题保底；到期题再多也只安排当天配额内的量，超出的自动顺延到后续天，杜绝「墨墨背单词式」长时间不练后一次性涌出大量到期题。
- **逾期重学标记**：新增 `isStaleReview`——超过 30 天未复习的题标记为「重新学」，从短间隔重新开始，不留痛苦长尾。

### 🐛 优化
- **首页记忆卡片改显示「今日建议 N 题」**：不再显示全量到期数，并标注顺延题数，从源头消除复习焦虑。

### 🔧 重构
- 记忆复习页 `dueList` 按配额截断（越弱越优先），顺延部分清晰提示「每天轻量复习，不会堆积」。

---

## [1.2.34] - 2026-08-23

### ✨ 新功能
- **记忆复习（间隔重复）**：新增 `/memory-review` 页面，基于艾宾浩斯遗忘曲线 / SM-2 算法管理复习计划。任何练习模式答题都会自动积累记忆数据（自动评估记忆质量，可手动覆盖为「认识/一般/模糊/不认识」），按 `next_review` 到期生成今日待复习队列，并提供记忆强度分布（强/中/弱/未追踪）与记忆健康度概览。
- **首页记忆复习入口**：首页新增「🧠 记忆复习」卡片，展示今日待复习题数与记忆健康度，点击直达记忆复习页。

### 🐛 修复
- **学习计划复习判断失效**：修复 `calculateDailyTask` 用驼峰字段（`questionId/lastReview`）与实际存储（snake_case `question_id/last_review`）不匹配导致复习题永远不触发的问题；统一以 `next_review` 为准判断是否到期。

### 🔧 重构
- **复习记录写入统一**：`PracticeView` 的复习记录写入从「仅计划模式」改为「全局」，计划模式与普通练习共用同一函数，避免双写冲突。

---

## [1.2.23] - 2026-08-21

### 🐛 修复
- **ExamTakeView 查询码错误处理**：改进查询码回看错题的错误处理，区分网络异常、考试不存在、超时等不同情况，提供更详细的错误提示
- **WrongView 练习退出反馈**：错题本练习模式下，当题目数据异常或已删除时，添加 toast 提示「题目数据异常或已删除，已退出练习模式」
- **QuestionCard 选项乱序稳定性**：将选项乱序映射（displayMap）改为 computed 属性，使用固定种子确保同一题目乱序结果一致，避免组件属性变化时映射不同步

### 🔧 优化
- **代码质量改进**：修复3个潜在bug，提升用户体验和代码健壮性
- **错误信息优化**：提供更友好的错误提示，帮助用户理解问题原因

---

## [1.2.22] - 2026-08-14

### 🐛 修复
- **按题型排序生效**：修复创建考试时题目被 `shuffle()` 打乱、覆盖排序的问题。现在 `createExam()` 和综合抽题考试都按「单选 → 多选 → 判断」有序排列，不再随机打乱。
- **判断题类型规范化**：`createExam()` 抽题时自动将云端判断题（存储为 `type:'single'` + `["正确","错误"]`）规范化为 `type:'judge'` + `answer:'true'/'false'`，确保判分和排序正确。
- **综合抽题内容识别**：MixExamView 的题型配比抽题改用 `classifyQuestionType()` 内容识别（替代仅检查 type 字符串），与考试模块保持一致。

### ✨ 新功能
- **模板生成考试**：创建考试页新增「📋 模板生成」模式，按智能组卷模板（5 等级 × 3 题型 = 220 题）一键生成正式考试。与手动创建共享表单（名称/说明/时长/可见范围/选项乱序/截止时间），创建后生成查询码可分享给考生。

### 🔧 重构
- **COMPOSE_SPEC 统一导出**：智能组卷配额模板从 ComposeExamView 本地定义改为 exam.ts 统一导出，ComposeExamView 和 ExamCreateView 模板生成模式共用。

---

## [1.2.21] - 2026-08-14

### ✨ 新功能
- **考试自动下一题**：综合抽题(MixExamView)、智能组卷(ComposeExamView)、本地练习考试模式新增「自动下一题」开关。开启后：
  - 单选题：点击选项即自动提交并跳转下一题，无需点「确认答案」
  - 判断题：点击选项即自动提交并跳转下一题
  - 多选题：选完按「确认答案」后自动跳转下一题
  - 考试模式下不区分答对答错（交卷后才看成绩），答完即跳；练习模式下仅答对才自动跳

---

## [1.2.20] - 2026-08-14

### 🔧 调整
- 🔒 **考试模式禁用 AI/收藏/编辑**：QuestionCard 在考试模式（examMode）下隐藏工具栏的 AI 解析(🤖)、收藏(☆)、编辑(✎) 按钮及 F 快捷键，防止考试作弊。
- **修复题号导航答案泄露**：综合抽题(MixExamView)和智能组卷(ComposeExamView)的题号导航在交卷前会以绿/红显示对错——现改为交卷前只显示「已答」(浅蓝)，交卷后才显示对错。
- **创建考试支持按题库×题型配额**：手动创建考试勾选「按题型配比抽题」后，每个选中的题库可分别设置单选/多选/判断数量（原来全局统一），更灵活地组卷。

---

## [1.2.19] - 2026-08-14

### 🔧 调整
- 🎲 **智能组卷并入「创建考试」页**：移除考试列表独立的「🎲 智能组卷」按钮，改为在「创建考试」页顶部增加模式切换（📝 手动创建 / 🎲 智能组卷），统一入口。
- **答题过程不即时显示答案**：考试模式下，选完答案点「确认」后不再以颜色高亮泄露对错，仅标记「已作答」，只有交卷后才出成绩。
- **按题型排序、不打乱**：智能组卷生成的 220 题按「单选(80) → 多选(40) → 判断(100)」顺序排列，不再随机打乱。

---

## [1.2.18] - 2026-08-14

### ✨ 新增
- 🎲 **智能组卷 · 综合大考**：考试管理页新增「智能组卷」入口，按「等级 × 题型」配额（初级 11/5/14、中级 11/5/14、高级 29/15/36、技师 18/10/22、安规 11/5/14 = **220 题**）从公共题库随机抽题合成综合卷，**每次开始重新组卷、每场不同**。默认时长 180 分钟可调，交卷显示总分 + 分等级成绩，可「再考一次」换新卷。
- 实现要点：新增 `classifyQuestionType()`（内容识别题型，兼容云端判断题 `type:'single'`+`["正确","错误"]` 的存储），并修复 `createExam` 题型配比模式抽不到判断题的隐患。

---

## [1.2.17] - 2026-08-10

### 🔧 调整
- 📜 **每日一诗（替代随机古诗）**：访问栏下方改为「每日一诗」——按本地日期确定性取一首（当天稳定、次日换一首轮转），卡头显示「📜 每日一诗 · 2026年8月10日 星期一」。
- **题材筛选为激励学习向**：剔除原 28 首中的思乡/送别/纯写景，替换为专注「劝学 / 惜时 / 坚韧 / 进取 / 悟理」的诗词（如《劝学》《冬夜读书示子聿》《长歌行》《登鹳雀楼》等，共 16 首），呼应「激励做题与学习」。数据见 `src/lib/poems.ts`。

---

## [1.2.16] - 2026-08-10

### ✨ 新增
- 📜 **首页随机古诗**：访问栏下方新增古诗卡，每次打开首页随机展示一首公共领域古典诗词（含标题 / 作者 / 朝代），数据见 `src/lib/poems.ts`（28 首唐诗宋词等）。纯前端、无云端依赖，渲染失败静默不影响首页。

---

## [1.2.15] - 2026-08-10

### 🐛 修复
- **访问统计计数恒为 0**：`incField` 原仅靠 `update` 抛异常触发 `set` 创建文档的兜底；但 CloudBase Web SDK 对「不存在的文档」调用 `update` 返回 `updated:0` 且不抛错，导致兜底永不触发、计数文档永远建不出。改为显式检查 `updated===0` 再 `set` 创建（同时保留异常兜底），计数正常落库。

---

## [1.2.14] - 2026-08-10

### ✨ 新增
- 👁 **访问统计**：首页顶部显示「累计访问次数 · 今日访问次数」。独立云端集合 `visit_stats` 原子自增计数，本设备按天去重（刷新不重复计）；未配置云同步的访客也能通过默认 envId 匿名登录贡献计数；集合 ACL 需设为「所有用户可读写」

---

## [1.2.13] - 2026-08-09

### ✨ 新增
- 📥 **导入恢复**：设置页数据区新增「📥 导入恢复」按钮，选择本地备份 JSON 即可整体恢复全部数据（题库 / 题目 / 错题 / 收藏 / 练习记录 / 设置），恢复前二次确认防误操作

### 🔒 优化
- 数据备份升级为**全量备份**：「💾 立即备份」现在一次性导出题库 + 题目 + 错题库 + 收藏 + 练习记录 + 设置（旧版仅导出题库列表与设置，缺失题目等核心数据）

---

## [1.2.12] - 2026-08-09

### ✨ 新增
- 手机版（≤768px）右上角导航栏新增「🔄 刷新」按钮（与侧边栏刷新同逻辑：清 Service Worker 缓存 + 硬刷新，确保拿到最新版本）

### 📝 优化
- 考试列表「📖 预览」按钮文案改为「📖 进入」，更准确表达进入考试

---

## [1.2.11] - 2026-08-09

### ✨ 新增
- 📥 **公共题库「添加到我的题库」**：首页每个公共题库卡片新增「＋ 添加到我的题库」按钮，一键将云端题库导入为本地私人副本，导入后自动获得进度条 / 错题本 / 收藏夹全套功能（导入时显示分批进度）
- 导入完成后卡片显示「✓ 已添加到我的题库」绿色标记

### 📝 优化
- 公共题库小标题增加提示文案：「此处仅供展示，练习数据不保存，点『添加到我的题库』导入本地后享进度/收藏/错题」，引导用户导入到本地

### 🔧 工程约定
- 导入副本一律建为 `private`，避免被云同步当成公开题库推到云端污染公共库
- 写入前剥离公共题自带的 `id`/`bank_id`，避免与本地自增主键冲突
- 大题库（合集 2951 题）按 250 题/批写入，防单事务过大卡死

---

## [1.2.10] - 2026-08-09

### 🐛 变更
- 「联系我」弹窗二维码占位图替换为用户提供的真实微信二维码（`public/wechat-qr.png`）
- 弹窗文案改为「扫码添加微信」

---

## [1.2.9] - 2026-08-09

### 🐛 修复 / 优化
- 删除侧边栏无效的「关闭」按钮（Web 版 `quitApp` 本就不生效）及其残留函数/样式
- 刷新按钮加文字标签「🔄 刷新」+ 提示文案「加载不全或卡顿，点此刷新」

---

## [1.2.8] - 2026-08-09

### ✨ 新增
- 侧边栏底部新增「联系我」入口（圆形头像 + 文字），点击弹出微信二维码弹窗

---

## [1.2.7] - 2026-08-09

### 🎨 视觉
- 主 logo 更换为兔子动态 logo：从 `D:\logo` 现成 AE 动画（logo2.gif）提取绿色圆形 + 白色兔子剪影，生成多尺寸 PNG（32/64/192/256/512）+ 透明动态 `logo.gif`
- 更新 favicon、顶栏 logo、PWA manifest 图标引用

---

## [1.2.6] - 2026-08-09

### 🎨 视觉
- 首页图标全部改为轻量 GIF 动图（书本/火箭/骰子/火焰，96×96 透明背景 3 帧浮动），提升交互活力

---

## [1.2.5] - 2026-08-09

### 🐛 修复
- 修复交卷后显示「试卷不存在」、查不到查询码的问题（交卷后 `exam=null` 与成绩页分支顺序导致成绩页被 `!exam` 分支抢先命中）

---

## [1.2.4] - 2026-08-09

### 🐛 修复
- 修复创建考试选公共题库时「选一个全选、改一个数量其它全变」（云端文档无 `id` 字段，v-for key 全 undefined 导致 DOM 复用污染；改用 `bankKey(b)` 兼容 `_id`）

---

## [1.2.3] - 2026-08-09

### 🐛 修复
- 修复全新访客（零配置）首页看不到公共题库/公共考试：`ensureCloud()` 无配置时自动回退默认 `DEFAULT_CLOUD_ENV_ID` 只读初始化，链接可放心发给别人

---

## [1.2.2] - 2026-08-09

### 🐛 修复
- 修复自建题库「题目框消失、题号导航还在」（QuestionCard 中 IIFE 在 `rawOptions` 声明前访问引发 TDZ 崩溃，调整 computed 声明顺序）

---

## [1.2.1] - 2026-08-09

### 🐛 修复
- 公共题库刷题页题型筛选：判断题（云端存为 single + 选项[正确,错误]）此前无「判断」按钮且全不选会白屏；统一 `normType` 规范化识别，加空筛选守卫防白屏
- 自建题库练习页筛选无效：筛选后未同步重建答题顺序，修复 `rebuildOrderFromFilter`

---

## [1.2.0] - 2026-08-09

### ✨ 新增
- 新增「选项乱序」功能：题库练习 / 公共题库刷题可手动开启打乱选项顺序，模拟考试默认开启
- 乱序后判分、答案高亮、存档自动映射对齐，不会出现「显示 A 判成 C」错位

---

## [1.1.1]

### 🐛 修复
- 修复公共题库打开无反应：加载增加超时保护与失败重试提示，不再卡在「加载中」
- 修复公共题库题目被截断的问题：合集 / 技师等超过 500 题的题库现在可刷到全部题目
- 公共题库加载失败时显示具体原因和「重试」按钮

---

## [1.1.0]

### ✨ 新增 / 修复
- 修复考试三大问题：无法删除、交卷后显示考试不存在、交卷报 [object Object]
- 新增考试本地快照兜底：网络异常也能查看和回看已参与的考试
- 关闭启动自动云同步，消除刷题卡顿；改为手动「立即同步」
- 新增首页公共题库与公共考试入口，无需同步即可直接刷公共题
- 新增公共题库直刷模式：顺序 / 随机 / 模拟考试三种刷题方式
- 新增管理员面板（本机管理服务）：密码保护，可管理公共考试 / 公共题库 / 清理个人数据
- 云端公共题库数据重建：6 大题库（合集 / 初级 / 中级 / 高级 / 技师 / 安规）共 7049 题与权威源一致
- 技师题库按权威 TXT 重建（820 题：单选 292 + 多选 192 + 判断 336），技师模拟考试重新抽题
- 错误提示统一优化：不再出现 [object Object] 等无意义报错

---

## [0.1.8]

### 🐛 修复
- 修复 mammoth 提取 docx 时选项分隔符错位导致部分题目选项被并入题干的问题（如习思想2第101题）
- PDF 导入改为异步执行，支持取消按钮和逐页进度反馈
- PDF 导入添加 5 分钟硬超时保护，单页失败自动跳过
- 答案区识别优化，缺少小节标题的章节可基于内容自动推断题型

---

## [0.1.1]

### 🎨 视觉
- 更新应用图标（全平台）
- 修复 WebView 缓存导致旧图标不刷新的问题
- 修复 exe 文件图标不显示的问题（多尺寸 ICO 嵌入）

---

## [0.1.0]

### 🎉 首发版本
- 支持 docx / txt / md / pdf 导入
- AI 自动解析（多模型：智谱 / DeepSeek / Agnes）
- 收藏夹 / 错题本 / 学习热力图
- 题目导航 / 搜索高亮 / 题型筛选
- 自动更新支持

---

## 说明
- 刷题宝网页版（PWA）由桌面版（Tauri）重构而来，数据层由 SQLite 改为浏览器 IndexedDB，支持离线刷题与 PWA 安装
- 公共题库/公共考试数据来自 CloudBase 云端（匿名只读），链接零配置即可分享
